# Kernel modules to include in the microboot initramfs, one per line. Comments
# and blank lines are ignored. Each entry is resolved via dracut-install.
# List only what you want - not transitive dependencies.
ipv6
